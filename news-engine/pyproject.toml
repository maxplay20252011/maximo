[project]
name = "news-engine"
version = "0.1.0"
description = "Motor de mapeo noticias -> exposicion de activos"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
